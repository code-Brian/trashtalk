import { LoginForm } from '../components/LoginForm'

export const LoginPage = () => {
    return (
        <div>
            <h1>Login to talk trash!</h1>
            <LoginForm/>
        </div>
    )
}
